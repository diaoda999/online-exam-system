<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.model.mapper.QuestionBankMapper">

    <!-- 分页查询题库列表（关联创建者名称） -->
    <select id="selectBankListWithCreator" resultType="com.exam.model.vo.bank.BankVO">
        SELECT
            qb.id,
            qb.bank_name AS bankName,
            qb.description,
            u.real_name AS creatorName,
            qb.question_count AS questionCount,
            qb.create_time AS createTime,
            qb.update_time AS updateTime
        FROM question_bank qb
        LEFT JOIN user u ON qb.creator_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (qb.bank_name LIKE CONCAT('%', #{keyword}, '%')
                     OR qb.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY qb.create_time DESC
    </select>

</mapper>
