spring:
  profiles:
    active: dev

server:
  port: 8080

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.exam.model.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ExamSystemSecretKeyForJwtTokenGenerationMustBeLongEnough2024OnlineExam
  expiration: 86400000
